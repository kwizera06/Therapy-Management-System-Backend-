spring.application.name=Therapy-Management-System-backend
server.port=8080

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-d5njjju3jp1c73apc2eg-a.oregon-postgres.render.com:5432/tms_db_wzhb}
spring.datasource.username=${DB_USERNAME:tms_db_wzhb_user}
spring.datasource.password=${DB_PASSWORD:gFsgV72RdJnWYYbWulPI9eBhMiPnYHB9}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# JWT Configuration
app.jwtSecret=${JWT_SECRET:mySecretKeyForJWTTokenGenerationAndValidation123456789}
app.jwtExpirationMs=86400000

# Email Configuration (SMTP)
# Replace with your email credentials
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:kwizeraolivier06@gmail.com}
spring.mail.password=${MAIL_PASSWORD:bfmw fxef rocf avri}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Frontend URL
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
